steps:
  - key: outline
    promptTemplate: Build the outline.
    outputSchema:
      type: object
      properties:
        summary:
          type: string
  - key: scenes
    promptTemplate: Build the scenes.
    outputSchema:
      type: object
      properties:
        scenes:
          type: array
